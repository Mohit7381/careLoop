{
  "finding_rank": 1,
  "repo": "timor/oms",
  "found": true,
  "file": "src/main/java/com/halodoc/timor/oms/service/factory/impl/BaseCancellationTypeAdapterService.java",
  "line": 298,
  "snippet": "    @Override\n    public Order abandonOrderV2(final String customerOrderId, final CancelRequest cancelRequest) {\n        final String lockKey = customerOrderId;\n        final boolean isLockAcquired = lockUtil.lock(lockKey);\n        try {\n            if (isLockAcquired) {\n                final Order order = getOrderService().findbyCustOrderIdFromDao(customerOrderId);\n                checkPaymentStatus(order);\n                getOrderService().setRefundToWalletAttribute(order);\n                if (!order.canCancel(cancelRequest)) {\n                    log.error(\"Order with id = {}, status = {} not in valid to abandon\", customerOrderId, order.getStatus());\n                    throw new HalodocWebException(ORDER + customerOrderId + \" cannot be abandoned\", \"3011\", CONSTRAINT_VIOLATION, 422);\n                }\n                if (!order.shouldSwallowAbandonAction()) {",
  "searches_run": 3,
  "_verified": "Live GitLab search 2026-09-03, project id 61 (timor/oms). CORRECTED 2026-09-03 (Harshit, PR #3): traced the FULL real call chain \u2014 the timer-driven AbandonOrderService.abandonOrder() (service/scripts/AbandonOrderService.java:37) queries OrderDao.getTimorOrdersListToAbandon() (dao/OrderDao.java:505, orders stuck in approved/payment_processing/payment_failed past OrderAbandonConfiguration.timeToAbandonOrderInMinutes), then calls abandonOrderV2() for each (inherited by both CartAbandonAdapterService and SystemAbandonAdapterService). Read the entire method body: it reverses benefits/rewards/payment-links/delivery-fee and marks the order failed, but calls ZERO notification/communication methods. Verified via grep: 'garuda' absent (case-insensitive, zero matches in the full raw file despite Garuda integration classes existing elsewhere in the service); 'sendCommunication' found at line 216, but that's inside the unrelated cancelOrderAndNotifyUser method, 82 lines away \u2014 the capability exists in the file but isn't wired into this mechanism. This SUPERSEDES the earlier cancelOrderAndNotifyUser:208 candidate, which was a sibling method never actually called by the timer job \u2014 this is now a clean, fully confirmed gap like GAP 1, not a weaker candidate."
}
